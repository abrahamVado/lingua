name: 'PDS Recipe: Slider Banner'
type: module
description: 'Hero slider banner with Slick. Editable slides (desktop/mobile image, title, intro, CTA) exposed as a Drupal block.'
package: 'PDS Recipes'

core_version_requirement: '^10 || ^11'
# If your site is on Drupal 10.2.x and it yells about ^10.3 you can loosen.
# Keep this aligned with the rest of your custom recipes.

dependencies:
  - drupal:system
  - drupal:block
